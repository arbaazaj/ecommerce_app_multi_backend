// Base theme configuration

abstract class AppThemeBase {
  static const double defaultPadding = 16.0;
  static const double borderRadius = 12.0;

  // Shared text styles, dimensions, etc. can go here.
}
